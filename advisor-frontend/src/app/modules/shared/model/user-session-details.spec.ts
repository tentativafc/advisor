import { UserSessionDetails } from './user-session-details';

describe('UserSessionDetails', () => {
  it('should create an instance', () => {
    expect(new UserSessionDetails()).toBeTruthy();
  });
});
